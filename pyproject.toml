[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "data_ingestion_project_2025"
version = "0.1.0"
description = "Projet de pipeline d’ingestion de données"
